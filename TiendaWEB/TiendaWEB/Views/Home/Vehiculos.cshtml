@{
    ViewBag.Title = "Registro de Vendedores";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="content-wrapper d-flex align-items-center auth px-0">
    <div class="row w-100 mx-0">
        <div class="col-lg-4 mx-auto">
            <div class="auth-form-light text-left py-5 px-4 px-sm-5">
                <h1 style="font-size: 30px;"> Registro de vendedores </h1>
                @using (Html.BeginForm("RegistrarVendedor", "Vendedores", FormMethod.Post, new { @class = "pt-3" }))

                {

                    <div class="form-group">
                        @Html.TextBox("Estado", null, new { @class = "form-control form-control-lg", placeholder = "Cédula" })
                    </div>
                    <div class="form-group">
                        @Html.TextBox("Estado", null, new { @class = "form-control form-control-lg", placeholder = "Nombre" })
                    </div>
                    <div class="form-group">
                        @Html.TextBox("Estado", null, new { @class = "form-control form-control-lg", placeholder = "Correo Electrónico" })
                    </div>
                    <div class="form-group">
                        @Html.TextBox("Estado", null, new { @class = "form-control form-control-lg", placeholder = "Estado" })
                    </div>
                    <div class="mt-3">
                        <input class="btn btn-block btn-primary btn-lg font-weight-medium auth-form-btn" value="Registrar Vendedor">
                    </div>
                }
            </div>
                </div>
            </div>
        </div>
